## PATH: app/main.py
from __future__ import annotations

from typing import Any

from Graph.graph import get_graph
import memory_store

# ============================================================
# پرامپت سیستمی Agent -- شخصیت "مدیر ارشد" طبق سند معماری.
# ============================================================

AGENT_SYSTEM_PROMPT = """
تو مدیر ارشد (Agent) یک سیستم تحلیل هوشمند کسب‌وکار هستی که به مدیران
یک فروشگاه آنلاین در تحلیل داده کمک می‌کنی. ابزارهای زیر در اختیار
داری:

    tool_sql   -- خودت مستقیم یک کوئری SQL معتبر Postgres می‌نویسی
                  (اسکیما و قوانینش در description خود ابزار آمده)
    tool_rag   -- جست‌وجوی معنایی خلاصه‌شده در نظرات مشتری‌ها (فقط
                  search_topic و اختیاراً product_id -- بدون فیلتر
                  برچسبی/جنبه‌ای، چون مدل ABSA ما روی استخراج جنبه دقت
                  پایینی داره)
    tool_chart -- ساخت نمودار واقعی (Chart.js/ECharts/Plotly) از روی یک
                  کوئری SQL که خودت می‌نویسی

قوانین کار تو:

۱. حافظه‌ی مکالمه را همیشه اول چک کن. اگه جوابِ بخشی از سوال از تاریخچه‌ی
   همین مکالمه (پیام‌ها و نتایج ابزارهای قبلی) قابل استخراجه، دوباره
   ابزاری که همون داده رو قبلاً آورده صدا نزن.
   مثال دقیق: اگه در دور قبلِ همین مکالمه با tool_sql تایید کردی که
   فروش محصول Y "افزایشی" بوده و کاربر الان فقط پرسیده "چرا؟"، دیگر
   لازم نیست دوباره با tool_sql جهت (افزایشی/کاهشی) رو چک کنی --
   مستقیم و فقط با tool_rag (با جهت مثبت/افزایشی در search_topic، مثلاً
   "دلایل رضایت و استقبال از محصول Y") برو سراغ دلیل.

۲. اگه سوال هم به داده‌ی عددی هم به دلیل/احساس کیفی نیاز داره و هنوز
   هیچ‌کدوم از قبل در تاریخچه نیست (مثلاً "سود برند X و احساسات مردم
   درباره‌ش رو بگو")، هر دو ابزار (tool_sql و tool_rag) رو در همون دور،
   هم‌زمان صدا بزن.

۳. برای سوالات آماری ساده (بدون "چرا")، فقط tool_sql کافیه.

۴. برای سوالات علّی از نوع "چرا فروش/امتیاز/بازدید محصول X کم/زیاد شده؟"،
   این ترتیب دقیق را رعایت کن -- هیچ‌کدوم از این مراحل رو جابه‌جا یا حذف
   نکن:
       الف) اول، و فقط با tool_sql، تایید کن که واقعاً همون تغییری که
            کاربر فرض کرده (کاهش یا افزایش) اتفاق افتاده -- مثلاً فروش
            محصول X را در بازه‌ی اخیر با بازه‌ی قبل مقایسه کن.
       ب) اگر تغییری که کاربر فرض کرده اصلاً رخ نداده (مثلاً فروش کاهش
          نداشته)، همین‌جا متوقف شو و صادقانه در جواب نهایی بگو که این
          فرض درست نیست -- به‌هیچ‌وجه سراغ tool_rag نرو، چون رفتن سراغ
          دلیلِ چیزی که اصلاً اتفاق نیفتاده گمراه‌کننده است.
       ج) اگر تغییر تایید شد، با tool_rag (search_topic در همون جهت،
          یعنی برای کاهش: "دلایل نارضایتی و شکایت از محصول X"؛ برای
          افزایش: "دلایل رضایت و استقبال از محصول X"، به‌همراه product_id
          همون محصول) نظرات مرتبط رو خلاصه‌شده بگیر.
       د) در جواب نهایی، عدد/درصد تغییر (از SQL) را با مضامین/نظرات
          خلاصه‌شده (از RAG) ترکیب کن -- ولی هرگز رابطه‌ی علّی‌ای که
          مستقیم از این دو شاهد برنمیاد رو اختراع نکن؛ اگه نظرات کافی
          برای توضیح افت/رشد نبود، صریح بگو شواهد کافی برای یک دلیل
          قطعی نیست.

۵. اگه ابزاری خطا داد (مثلاً SQL نامعتبر یا داده‌ای پیدا نشد)، خودت با
   یک tool_call جدید و پارامتر/کوئری اصلاح‌شده دوباره تلاش کن؛ کاربر
   نباید متوجه این خطای داخلی بشه مگر واقعاً بعد از چند تلاش هم به
   داده‌ای نرسیدی.

۶. فقط وقتی کاربر صریحاً نمودار/چارت/داشبورد/مصورسازی خواست از tool_chart
   استفاده کن.

۷. جواب نهایی رو همیشه به فارسی، روان، و در قالب یک پاسخ انسانی و
   مدیریتی بنویس -- نه دامپ خام JSON نتایج ابزارها.
"""

# ============================================================
# قانون ۸ -- غیرفعال تا آماده شدن tool_knowledge_base.
# ============================================================
# وقتی knowledge_base_agent.py پیاده‌سازی شد و tool_knowledge_base در
# tools.py از حالت کامنت خارج شد، این بلوک رو هم از حالت کامنت خارج کن
# و به انتهای AGENT_SYSTEM_PROMPT بالا اضافه کن (یا مستقیم رشته‌ها رو
# با + به هم بچسبون). تا اون‌موقع عمداً این‌جا نگه داشته شده، نه داخل
# پرامپت زنده -- چون اگه پرامپت از ابزاری حرف بزنه که در TOOL_DEFINITIONS
# ثبت نشده، مدل سعی می‌کنه صداش بزنه و با خطای "ابزار ناشناخته" مواجه
# می‌شه.
#
# KNOWLEDGE_BASE_RULE = """
# ۸. قبل از دادن هرگونه پیشنهاد یا توصیه‌ی مدیریتی (نه فقط گزارش عدد/
#    نظرات، بلکه وقتی کاربر می‌خواد بدونه "چیکار کنم؟")، حتماً اول
#    tool_knowledge_base رو با موضوع مرتبط صدا بزن و پیشنهادت رو با
#    ترکیب اون دانش آموزشی + دانش عمومی خودت بساز -- نه فقط از حافظه‌ی
#    خودت. اگه پایگاه‌دانش چیز مرتبطی نداشت، صریح بگو و بر پایه‌ی دانش
#    عمومی خودت پیش برو.
# """


def run(
    question: str,
    chat_id: str | None = None,
    history: list[dict[str, Any]] | None = None,
) -> dict[str, Any]:
    """
    question: سوال جدید کاربر.
    chat_id:  اگه بدی، تاریخچه‌ی این چت خودکار از Postgres خونده می‌شه
              (memory_store.load_messages) و بعد از پایان اجرا خودکار
              ذخیره می‌شه (memory_store.save_messages) -- شامل
              فشرده‌سازی خودکار وقتی مکالمه طولانی بشه (maybe_compact).
    history:  اگه بخوای خودت تاریخچه رو مدیریت کنی (مثلاً لایه‌ی API
              خودت جای دیگه‌ای نگهش می‌داره)، مستقیم بده -- در این حالت
              chat_id فقط برای ذخیره‌سازی نهایی استفاده می‌شه، نه خواندن.
              اگه هیچ‌کدوم رو ندی، دقیقاً مثل قبل -- بدون حافظه‌ی
              پایدار -- کار می‌کنه.
    """
    if chat_id and history is None:
        history = memory_store.load_messages(chat_id)

    messages: list[dict[str, Any]] = list(history) if history else []

    if not messages or messages[0].get("role") != "system":
        messages.insert(0, {"role": "system", "content": AGENT_SYSTEM_PROMPT})

    messages.append({"role": "user", "content": question})

    # فشرده‌سازی فقط وقتی واقعاً از سقف دورها رد شده باشیم اثر می‌کنه --
    # وگرنه همون messages بدون تغییر برمی‌گرده (هزینه‌ی این خط تقریباً صفره).
    messages = memory_store.maybe_compact(messages)

    graph = get_graph()
    result = graph.invoke({"messages": messages, "iterations": 0})

    if chat_id:
        memory_store.save_messages(chat_id, result["messages"])

    return result


def main() -> None:
    result = run("نظرات کاربران درمورد دفترچه یادداشت ها چطوره؟ افزایش فروش داشتیم؟", chat_id="demo-chat-1")
    print("\nFINAL ANSWER:")
    print(result.get("final_answer"))

    print("\nVALIDATION:")
    print(result.get("validation"))

    errors = result.get("errors") or []
    if errors:
        print("\nERRORS:")
        for err in errors:
            print(f"  - {err}")

    followup = run("چرا؟", chat_id="demo-chat-1")
    print("\n\n--- سوال ادامه‌دار با حافظه‌ی Postgres (chat_id) ---")
    print(followup.get("final_answer"))

    chart_result = run("نمودار فروش ماهانه دفترچه یادداشت ها رو نشون بده")
    print("\n\n--- نمونه‌ی نمودار ---")
    print(chart_result.get("final_answer"))


if __name__ == "__main__":
    main()
